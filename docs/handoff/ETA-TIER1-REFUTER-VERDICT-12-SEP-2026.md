# ETA TIER 1 REFUTER VERDICT — 12 Sep 2026

Diff `d4821ec..vinay/tier1-states` (7 commits, 30 files, +3888/−125) against kickoff §1–§5 and the four fix-up rulings. No source file changed by me.

**Gates, all rerun here.** `npm test`: `Tests 1772 passed (1772)`, 79 files. `npx tsc --noEmit` exit 0. `npm run build` exit 0. `npm run check:silent`: the same 9 accepted findings, none in a changed file. `swift build`: `Build complete!`. `swift test` (0.1.8 runbook flags): `Test run with 600 tests in 48 suites passed`, zero `needsEnrolment`. The builder's `RetainedArchiveRecoveryTests` flake did not recur in my run. New suites re-run alone: 93 passed (bench-room-states 52, bench-commands-new-kinds 18, mcp-room-command 9, room-install-assign-test-channel 7, mcp-diff-room-install-states 7) — the report's counts are exact.

**§4 acceptance.**
1. SILENT in `scribe_diff_room` — **PASS in code**, live run outstanding. Seam 1 is delivered: `room_state.flags` / `drift_since` at `lib/mcp/tools/bench.ts:2591`. Clears on the first non-silent poll (`silence_ms` → 0), well inside 10.
2. `channelLocked:true` + assign `stable` → card shows the lock, app stays on `test` — **PASS**. `RoomConfiguration.swift:571` guards on `!channelLocked`; the chip is `BenchInstallFleet.tsx` `data-channel-locked`.
3. `check_update_now` with 0.1.23 on `test` — **UNPROVEN**. Path present and unit-tested; needs a real 0.1.23 on the channel.
4. `report_diag` devices + last 100 lines, no secret — **PASS**. Default 100 (`reportDiagDefaultLines`), leak test executed.
5. `restart_engine` idle → listener gap < 15 s — **UNPROVEN live**. Exit 75 is correct for `KeepAlive {SuccessfulExit:false}`; the gap is a machine property.

**(a) 0.1.21 fallback — PASS.** `writeInstallState` runs on every poll with no version gate (`room-install.ts:1237`). With `silence_ms` null the evaluator takes `head.silent_polls >= 80`, carried in SQL from `pollIsSilent(zero_ratio ≥ 0.98)`; `pollClipped` falls back to `peak ≥ 0.99`.
**(b) paused/idle — PASS.** `recording = recordingSessionId !== null && !paused` (`bench-commands.ts:406`); SILENT, CLIPPING and ENCODER_STALLED are all gated on it, and ring entries carry `rec`.
**(c) LOAD-BEARING — PASS.** `PCMTailMeter.samplesSinceLoud` is a stored property written back on every `measure` (`RoomEngine.swift:3881`), so silence **accumulates across reads**; it resets only on a shorter file or a gap > 30 s of un-consumed durable audio. At the 1.5 s recording cadence a read consumes ~48 KB against a 960 KB reset window, so the reset never fires; `tape.pcm` is append-only for the life of a segment (no truncation in `publishAvailable`). 120 s of sub-−55 dBFS audio = 1,920,000 samples ÷ 16 = 120,000 ms. The server judges a 0.1.22 app by **raw `silence_ms ≥ SILENT_MS` (120000)**, ignoring the ring; ring run-length is the 0.1.21 path only. **The threshold is reachable — this is not a FAIL.**
**(d) CHANNEL_DRIFT under lock — PASS.** `bench-bus-constants.ts:648`: `channelLocked !== true` is the first term, and `driftSince` is left null, so the clock is not merely held but never started.
**(e) restart refusal — PASS.** `RoomEngine.swift:2894` returns `verbFailure("session_open")` → `ok:false` → server `status:"failed"` (`bench-commands.ts:540`). `restartRequested` arms only on `result.ok`, so a refusal never exits. When it does exit the code is 75, asserted non-zero at `RoomOperatorVerbTests.swift:229`.
**(f) secrets — PASS, with one naming gap.** The executed test serialises the whole `diag` and greps `eta_room_session`, `etaRoomSession`, `commandVerifyKey`, `SCRIBE_MCP_TOKEN`, the fixture JWT and the log values — all absent; `config` is encoded with `etaRoomSession = nil` and Swift's synthesised encoder omits the key. Server withholds on the same four (`DIAG_FORBIDDEN`, `bench-commands.ts:212`). `MIGRATION_SECRET` greps empty across the whole app (it is a server-only env var and nothing in `apps/room-recorder` reads it), but it is **not** in `DIAG_FORBIDDEN` — the implemented list is the kickoff's, which names `commandVerifyKey` in its place.
**(g) `test` round-trip — PASS.** Route accepts `"test"`; the poll returns `assigned === "stable" || "test"`; the clear is `CASE WHEN update_channel = assigned_channel THEN NULL ELSE assigned_channel END`, so a Mac reporting `stable` does not clear a `test` assignment, and a poll omitting the channel clears nothing (NULL comparison).
**(h) 0081 against a database at 0080 — PASS.** `bench_command.kind` carries exactly one CHECK after 0080 (`bench_command_kind_check`); `room_install.assigned_channel` carries 0079's one inline CHECK. Both DO blocks match on `conkey = ARRAY[attnum]`, refuse at `n_checks > 1`, and tolerate `n_checks = 0` (drop skipped). The re-add reuses 0079's implicit name inside the same transaction, which Postgres allows. All seven columns are `ADD COLUMN IF NOT EXISTS`, nullable, no DEFAULT.

**Defects.** None that breach the contract. Two things to carry:
- `lib/room-install-view.ts:795` — `assigned_pending` is still `assigned_channel === 'stable'`, so a pending **test** assignment renders no "channel assigned · waiting for the Mac" line. The builder flagged it; it needs a one-line ruling, and no card control assigns `test` today (API/MCP only).
- **Deploy ordering is load-bearing.** The poll UPDATE names `poll_ring`, `state_flags`, `clip_count`, `silence_ms`, `channel_locked`, `expected_device_name` unconditionally. If this server reaches an environment before 0081 runs, every native `applyInstallPoll` throws and is caught fail-open — recording is unaffected, but install rows stop updating entirely and assignments stop being delivered. 0081 must be applied **before** the code is promoted, not after.

**Safe to apply 0081 to preview and publish 0.1.22 to test — yes**, provided 0081 is applied before the server is promoted and 0.1.22 goes to `test` only (Home Office, Room 4.1), never `stable` before the partition steps.
