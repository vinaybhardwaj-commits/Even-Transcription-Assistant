# ETA TIER 2 SLICE B — REFUTER VERDICT (THIRD PASS) — 12 Sep 2026

Scoped to `a761ed1..73e344a` plus the item 6 sweep. Gates rerun by me: `npx tsc --noEmit` exit 0, no output; `npm test` **1930 passed (1930), 87 files** — both match the report. My work was four throwaway probe files (39 assertions) plus eight mutations of `lib/jobs/store.ts` and `types.ts`, each restored and confirmed with `git diff --exit-code`. Probes deleted; tree left exactly as found (five untracked handoff docs, nothing else). I did not edit non-test source, merge, push, or run a migration.

**1. The rewritten (a) test — PASS on the central claim.** I built my own mutation, textually invisible to every regex in the harness: the claim stamps a ZERO-LENGTH lease (`store.ts:99`, `Math.round(leaseMs / 1000) * 0`). The test fails, for the right reason — *"a second runner took the job at +200000ms"*, `refuter-b-queue.test.ts:190`. It observes real claim behaviour; it is not vacuous, and the Builder's account of its own proof is honest. **The order's bright line is tripped once, however:** widening the expiry to `lease_until < now() + make_interval(secs => 3600)` — a live lease treated as expired, the same hazard class — leaves the (a) test GREEN, caught only by a literal-text assertion at `tier2-jobs.test.ts:232`. I classify that as a defect in the fake (A6) rather than in the test; read strictly, the order makes it a FAIL, and the Orchestrator may rule that way.

**2. FAKE AUDIT — the class is not confined to the one the Builder found. Seven sites; five proven to mask a real clause.** Method: delete one clause from `store.ts`, run all five jobs test files (92 tests). Control — deleting `AND lease_owner = ${runner}` from `failJob` — DOES fail `refuter-b-queue.test.ts:283`, so the harness is not inert.
- **A1 `recordFailure`'s `AND status = 'running'` (`store.ts:266`) — MASKED, 92/92 still pass.** Both fakes hardcode `r.status !== "running"` (`tier2-jobs.test.ts:95`, `refuter-b-queue.test.ts:75`) instead of reading the clause, which the `done`/`failed` branches beside them do read via `guarded`. Delete it and a cancelled job's failure counter still climbs — the cancel-wins property the slice fought for, untested here.
- **A2 `recordFailure`'s terminality `CASE WHEN failures + 1 >= ${maxFailures}` (`store.ts:262`) — MASKED.** `>=` → `>` passes 92/92: the fakes compute terminality themselves (`if (Number(r.failures) >= Number(cap))`), so the cap could be off by one and (d) would still report `failures 3, status failed`.
- **A3 `saveStep`'s `lease_until = NULL` (`store.ts:138`) — MASKED.** Deleting it passes 92/92; both fakes null it regardless. "(b) a job with lease_until NULL is claimable" proves the fake, not the statement.
- **A4 `finishJob`'s `lease_owner = NULL` (`store.ts:157`) — MASKED.** Deleting it passes 92/92.
- **A5 the claim's `ORDER BY created_at` (`store.ts:92`) — MASKED.** Deleting it passes 92/92; both fakes sort by `created_at` on their own authority. FIFO is untested.
- **A6 the claimable predicate is matched as a SUBSTRING (`tier2-jobs.test.ts:64`, `refuter-b-queue.test.ts:52`).** The fakes read *whether* `lease_until IS NULL OR lease_until < now()` is present, then apply their own arithmetic — so any mutation that EXTENDS the expression still matches. This is item 1's escape.
- **A7 `owns` in `refuter-b-leak.test.ts:16-20` is DEAD CODE — and it masks something.** Fix-up 5 carefully rewrote it; it is called nowhere in that file, whose write branches grant every write. Wiring it in as the other two fakes do fails 4 of 12 tests. The cause: `refuter-b-leak.test.ts:110` calls `runOneStep(c[0]!)` with no runner — now REQUIRED — and `tsconfig.json:41` excludes `tests`, so `tsc` typechecks **0** test files (`--listFilesOnly` confirms). "Forgetting it is a compile error" holds only outside `tests/`. Under the production call shape that (e) narrative writes zero rows on every write and the job never reaches `done`; four tests in the repo's PHI-leak regression file currently assert nothing.

**3. The error gate — PASS as built. The `scribe_job_list` description — FAIL (`jobs.ts:155`).** I seeded the column by hand with nine shapes — unmapped prose, empty string, whitespace, `whisper_timeout:` (code-shaped, unpublished), a published code with a colon inside the prose, a code as suffix, leading whitespace, an uppercased code, and a raw Postgres `ERROR:` — and read each back through `scribe_job_status` AND `scribe_job_list` on a read-only token: in all 18 reads the prose never appeared and `error` was absent, and all 14 published codes round-trip as themselves. Invoke gets the raw column byte-for-byte through both tools, including when it is unmapped. `scribe_job_status`'s description (`jobs.ts:125`) now matches. `scribe_job_list`'s does not: it still reads "id, kind, status, step, attempts and timings" and never mentions `error_code` or that an invoke token gets prose from a listing — the report's "the description was rewritten" covers one tool, not both. Understated, not false; unlike Fix-up 4 it publishes no security claim the code fails to honour. Minor: `errorCodeOf` is still imported and unused at `runner.ts:21`.

**4. Owner semantics — PASS, with one report inaccuracy.** Against a row stamped by a real runner, `''`, `"   "`, a foreign runner and `null` each matched ZERO rows through all four writers (`saveStep`/`finishJob`/`failJob` returned 0, `recordFailure` returned null) and left `lease_owner`, `status` and `failures` untouched. A NULL-owned row is writable by nobody, including a null or empty-string caller. `saveStep` keeps the owner across three consecutive step boundaries and `finishJob` releases it. Two flags: an `''`-stamped row IS matched by an `''` writer, since `'' = ''` is TRUE in Postgres — unreachable today because `runClaimedBatch` mints `randomUUID()` (`runner.ts:142`), but `claimJobs` still accepts `runner: string | null = null` and would take `''`. And "only a terminal write releases it" is not true: `recordFailure` below the cap sets `lease_owner = NULL` on a row it leaves `running` (`store.ts:262`). Fail-safe in direction — the row then accepts no writer until reclaimed — but it is not what the report says.

**5. Margin floor — PASS.** `LEASE_MARGIN_MS` 0 fails (*"expected 0 to be greater than or equal to 30000"*) and 29999 fails (*"expected 29999 to be..."*). `lib/jobs/types.ts` restored, `git diff --exit-code` clean.

**6. Regression sweep — PASS on all three, with one coverage regression.** The three adopted files rerun green (37 tests). Re-proved independently at production constants: every claim in a `runClaimedBatch` invocation takes at most ONE row under a single minted runner id, and a live-leased row is untakeable at +0, +1, +MAX_STEP_MS and +LEASE_MS−1 and takeable at +LEASE_MS+1 — the batch/lease fault stays closed. A reclaimed row rejects `failJob`, `finishJob` and `saveStep` from its previous owner, keeping B's `step` and a null `error` — the stale-writer fault stays closed. Every registered kind refuses both a wrong-scope and a scopeless token at `scribe_job_submit`, queueing nothing — the scope-at-submit hole stays closed. Nothing in Fix-up 5 loosened them. The regression is A7: Fix-up 5's required-`runner` change silently hollowed out four tests in `refuter-b-leak.test.ts`. I re-proved that property myself under a correct call shape — a real `transcribe_range` job drives to `done` with no `lease_lost` and no transcript in the row or in a read token's answer — so this is lost coverage, not a live leak.

## Verdict

The product code is right. Everything the order asked me to break, I could not: no prose reached a read token through either tool in any of eighteen attempts, no degenerate or foreign owner wrote through any of the four writers, no kind accepted a token without its scope, no second runner took a live-leased row, and the happy path still completes under the stricter guard. Fix-up 5 closed both of my second-pass findings honestly, and the Builder's account of its own proof — including the admission that its first attempt passed for the wrong reason — is accurate.

What is left is harness strength, not behaviour. Five clauses of `store.ts` can be deleted with the whole jobs suite green (A1–A5), the claim predicate is only substring-matched (A6), and the file that exists to prove the PHI gate has four assertions that no longer run (A7). None of that is a hazard in this merge; all of it is why the NEXT regression will not be caught. A7 and the `scribe_job_list` description are the two I would fix first, and neither needs a migration. Every SQL string in this slice remains INFERRED and exercised only against fakes — `= ${runner}` on four writes and the `CASE WHEN failures + 1 >= ?` arithmetic most of all — and 0082 has still been run nowhere. `npm run build`, `npm run check:silent` and the Swift gate I did not rerun.

SAFE TO MERGE: yes
