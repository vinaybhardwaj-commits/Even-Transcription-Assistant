# ETA — HOTFIX REFUTER VERDICT — 12 SEP 2026
Scope `eba70f2..vinay/tier2-b-hotfix` (3537117, f584ae8). My reruns: `npx tsc --noEmit` exit 0; `npm run typecheck:tests` exit 0; `npm test` **89 files / 1953 passed**; `check:silent` still 9 accepted, none new. Tree restored to f584ae8 after every experiment.
**CENTRAL QUESTION — PASS.** Answered against the REAL shim `~/.local/bin/whisper-shim.py` (I ran my own throwaway copies on ports 18093/18095/18099; live :8081/:8080 untouched). `do_GET` serves only `/healthz`, else `send_error(404)` with no contact to :8080 — against a dead backend `GET /inference` gave **404** and the old predicate (`>=500 && !==501`) did not fire: original defect reproduced. With :8080 closed, `do_POST` raises an uncaught `ConnectionRefusedError` and shuts the socket with **no HTTP response** (`curl`→`000`); the new probe returns `{ok:false,reason:"transport"}` — **it FAILS**. Backend up but 500 "failed to load model" → shim forwards verbatim → `bad_status 500` — **FAILS**. Control with a healthy fake backend → `ok:true`, backend saw `multipart/form-data`. A dead or unloaded :8080 cannot produce a pass.
- **(a) PASS.** `42 -0.5 true false "  " [{}] [[1]] {}{} NaN undefined {'text':'x'}` all → `unparseable_body` (`lib/health/whisper-probe.ts:88`); a BOM passes only because `Response.text()` strips it. NOTE: a 200 body `{"error":"backend unavailable"}` **passes** — the probe asserts shape, not content. Latent only: `send_error` never returns 200 in this shim.
- **(b) PARTIAL FAIL.** A hang before headers → `whisper_timeout` with elapsed+budget, never throws. But an abort **during the body read** is swallowed by the inner catch at `lib/health/whisper-probe.ts:95` and misnamed `unparseable_body`. Proven; low likelihood (shim sends Content-Length).
- **(c) FAIL on the guard, covered elsewhere.** My revert A (constant swapped out): behavioural test *and* divergence guard both fail, right reasons. My revert B (`if (w.error === EMPTY_TRANSCRIPT) { void doneWith; }` + the assertion text left in a comment): **guard PASSES** — it is a text matcher and I defeated it; only the behavioural test caught the revert. Mutating `EMPTY_TRANSCRIPT` at `lib/mcp/tools/bench.ts:1562` alone: **all 6 hotfix tests stay green, the guard does NOT fail**; caught only by pre-existing `tests/unit/speech-turns-k5.test.ts:182`. Cause: the producer `lib/whisper.ts:320` still hard-codes the literal and bench.ts holds a copy, so "one name for one thing" (`lib/jobs/kinds/transcribe-range.ts:21`) is true of the two consumers only.
- **(d) PASS — none changed.** `transcribeStep` has one call site, its own dispatch (`lib/jobs/kinds/transcribe-range.ts:88`); `whisperNotOkAnswer` is module-private to bench.ts (`1733` def, `1912`/`1955` calls), untouched; `WhisperResult` is not in the diff; the success-path key SET is unchanged at 11 — only JSON key order moved.
- **(e) PASS.** `empty_transcript` absent from `JOB_ERROR_CODES` (`lib/jobs/errors.ts:24-41`); silent window → `done` with `silent_window:true, chars:0, segments:0`; `http_502` → `failed`/`whisper_failed` with the response body suppressed.
- **(f) fixture PASS, shape FAIL.** Fixture parsed: PCM, 1 ch, 16000 Hz, 16-bit, data 16000 B = **exactly 0.5 s**, peak 1500. I captured both requests on the wire: probe sends `file(health-probe.wav), response_format=json, temperature=0`; `transcribeWithWhisper` sends `file(audio.wav), response_format=verbose_json, temperature=0.0, beam_size=1, best_of=1`. "The same shape" at `app/api/health/route.ts:48` and `tests/unit/hotfix-whisper-probe.test.ts:87` overstates it.
**FLAGS (not blockers).** 1. The ffmpeg leg is still untested: the job path calls `transcribeWithWhisper(bytes,"audio/webm")` (`transcribe-range.ts:201`) → `audio.webm` → the shim transcodes; the probe's `.wav` name skips that. Broken ffmpeg = every real transcription 415s while `whisper: true`. 2. `/api/health` now runs a real inference per call and whisper.cpp serialises them, so a probe queued behind a long window can hit the 12 s budget and report `whisper:false` on a healthy busy server; the other probes use 8 s and the route has no overall budget. 3. A trailing-slash `WHISPER_BASE_URL` gives the probe `//inference` (prod strips it, `lib/whisper.ts:236`) — harmless in fact, CPython's `parse_request` collapses leading `//`; verified 200 on the real shim.
**SAFE TO MERGE: yes.**
