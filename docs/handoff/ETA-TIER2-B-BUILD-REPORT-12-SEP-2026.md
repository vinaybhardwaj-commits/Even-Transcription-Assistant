# ETA TIER 2 SLICE B BUILD REPORT — jobs — 12 Sep 2026

**Commit** `c5b5eb1` on `vinay/tier2-b`, branched from `vinay/release-b1` @ `aed0962`. Not pushed. `main` and `vinay/release-b1` untouched.

**Gates** (all four, final tree):
- `npx tsc --noEmit` exit 0.
- `npm test`: `Tests 1871 passed (1871)`, 84 files. Baseline `1838` in 82 — **+33 tests, +2 files**, none removed.
- `npm run build` exit 0 · `npm run check:silent`: the same **9** accepted findings, none in a Slice B file.
- No Swift gate: `apps/room-recorder` untouched. **0082 was not run anywhere; nothing published.**

**Files**: 19 files, +1774/−9. `lib/jobs/**`, `app/api/jobs/**`, `vercel.json`, `db/migrations/0082_scribe_job.sql`, `lib/mcp/tools/jobs.ts` — §1's Slice B row — plus three outside it, each required by the order: `lib/mcp/handler.ts` (register `JOB_TOOLS`), `lib/mcp/tools/bench.ts` (`async:true` on the two range tools), `lib/room-install.ts` (the `channel_reported` dedupe).

**What was built**
- **0082** `scribe_job` exactly as §3 lists it, plus `scribe_job_status_created_idx (status, created_at)`. `kind` is deliberately **not** CHECKed: a kind added by a later slice must not need a migration, and an unknown kind is refused at submit where the error can name the allowed list.
- **Runner** `POST|GET /api/jobs/run`, `maxDuration 300`. Claims ≤ 3, lease 240 s, one step per claim, `attempts > 3 → failed`. **GET as well as POST, because Vercel Cron issues GET** — a POST-only route would have been a cron that never fired. `CRON_SECRET` is accepted alongside `JOBS_RUNNER_SECRET` for the same reason. Secret unset → **503 and nothing runs**.
- **Kinds**: `transcribe_range` and `stitch` built; `audio_measure`, `emotion_clip`, `diarize_clip`, `stt_fanout`, `day_manifest` registered as stubs failing `not_implemented`, each with its real scope and a real `parseArgs`.
- **Tools**: `scribe_job_submit` (invoke), `scribe_job_status` (read), `scribe_job_list` (read), `scribe_job_cancel` (write). `scribe_transcribe_range` and `scribe_extract_audio` gained `async:true`; the synchronous path is untouched and stays the default.
- **Additions**: `scribe_audit_recent` (read, allow-listed actions, no free-text argument); `install.channel_reported` deduped to one row per (install_id, channel, minute).

**A defect the tests caught before it shipped.** `saveStep` releases the lease by nulling it, but the claim predicate required `lease_until IS NOT NULL` on the running branch — so **every job that successfully advanced a step became unclaimable for ever**, a queue that silently stopped after one step per job. The crash-resume test found it. A running row is claimable when nobody holds it: an expired lease **or** a null one.

**SQL (all INFERRED; no live database)**
- Claim: `WITH claimable AS (SELECT id FROM scribe_job WHERE (status = 'queued' OR (status = 'running' AND (lease_until IS NULL OR lease_until < now()))) ORDER BY created_at LIMIT ? FOR UPDATE SKIP LOCKED) UPDATE scribe_job j SET status = 'running', attempts = j.attempts + 1, lease_until = now() + make_interval(secs => ?), started_at = COALESCE(j.started_at, now()), updated_at = now() FROM claimable c WHERE j.id = c.id RETURNING j.<15 cols>`
- Step: `UPDATE scribe_job SET step = ?, progress = ?::jsonb, lease_until = NULL, status = 'running', updated_at = now() WHERE id = ? AND status = 'running'`
- Finish / fail / cancel: `UPDATE scribe_job SET status = 'done'|'failed'|'cancelled', … lease_until = NULL, finished_at = now(), updated_at = now() WHERE id = ?` — cancel additionally `AND status IN ('queued','running')` and RETURNs the row.
- Insert: `INSERT INTO scribe_job (id, kind, args, actor) VALUES (?, ?, ?::jsonb, ?) RETURNING <15 cols>`. List: `… WHERE (?::text IS NULL OR status = ?::text) AND (?::text IS NULL OR kind = ?::text) ORDER BY created_at DESC LIMIT ?`.
- Audit read: `SELECT action, actor_type, actor_id, target_type, target_id, metadata_json, created_at::text FROM audit_log WHERE action = ANY(?) AND (?::text IS NULL OR action = ?::text) AND created_at >= ?::timestamptz ORDER BY created_at DESC LIMIT ?`
- **`make_interval(secs => ?)`** is the one construct I could not test: a literal `now() + '240 seconds'::interval` would have been simpler but not parameterisable. Wants live eyes.

**Seams I interpreted differently from the spec**
1. **`transcribe_range` is THREE steps, not four.** §3 says resolve → join → transcribe → write. A fourth step would have to carry Whisper's **transcript** from `transcribe` to `write` through `scribe_job.progress` — patient speech, at rest, in a new table, readable by every token with `read` via `scribe_job_status`. So the write happens inside the transcribe step, which already holds the text, and `progress` never carries a word of it. **Needs a ruling if four steps were load-bearing.**
2. **The transcribe step does not yet write cues.** It returns the transcript in `result`; the scratch-graph turn-writer lives inside `scribe_transcribe_range`'s handler in `bench.ts`, which is outside Slice B's file table. Extracting it is a refactor, not a job. `dry_run` is carried on the result so a caller can see which it got.
3. **`stitch` reuses the tool's helpers rather than `scribe_stitch`** — §4.3 defines that tool in Slice C. This kind is the step machine only; the tool that submits it arrives with C.
4. **The runner answers GET.** §3 says POST; Vercel Cron only issues GET. Both verbs share one guard.
5. **`scribe_job_submit` passes `actor: null`.** The MCP tool handler is given `ToolArgs` and `ToolContext`, neither of which carries the resolved principal — wiring §2.3's actor through to a job needs a `registry.ts` signature change, outside this slice. Jobs submitted by a route or a later slice can set it; the column and the view already carry it.
6. **`install.channel_reported`'s dedupe window is 2 minutes on a per-minute key**, not 1. The key carries the minute, so the window only has to outlive one minute's polls; a genuine transition in the next minute writes its own row.
7. **The audit reader is allow-listed to five actions** and has no free-text argument. `audit_log.metadata_json` is written by many callers and this must not become a way to read whatever any of them stored.

**V's manual steps.** Apply 0082 to the preview before promoting, the way 0081 was: `curl -X POST -H "Authorization: Bearer $MIGRATION_SECRET" https://<preview-host>/api/run-migrations`. Set `JOBS_RUNNER_SECRET` on the deployment — **until it is set the runner answers 503 and no job ever runs**, which is the safe failure but is silent. `CRON_SECRET` optional.

**Subagents:** none.

## Fix-up 1 — on the three rulings
**(1) and (2) accepted as built**; seams 1 and 2 close. (2) is now stated where a caller will read it: `scribe_transcribe_range`'s description says `async:true` returns text and a `transcription_run` and **writes no turn cues — those arrive with Slice C**.
**(3)** `ToolContext` gains `actor: string` — the resolved `SCRIBE_MCP_TOKENS` actor, `mcp:`-prefixed once by `mcpActorId`, defaulting to `mcp:operator-v1` for the single-token fallback so a job's actor is never blank. `handler.ts` fills it from the principal it had already verified, and `scribe_job_submit` plus both `async:true` paths record it — replacing the `actor: null` seam 5 flagged.
**Tests +3** (1874, was 1871): a `SCRIBE_MCP_TOKENS` actor lands on the INSERT's actor parameter **end to end** (`checkMcpBearer` → `handleMcpRpc` → `submitJob` → `insertJob`) as `mcp:operator-v`; the fallback records `mcp:operator-v1`, never null; and the `ToolContext` shape is pinned.
**Worth knowing**: `tsconfig.json` EXCLUDES `tests`, so adding a required field to `ToolContext` was not a compile error in any test file — the existing `ctx` literals still pass `undefined` for it silently. The new test pins the type by reading the source instead. Gates: tsc 0 · **1874 passed (1874)** · build 0 · check:silent the same 9. Branch `vinay/tier2-b` pushed.

## Fix-up 2 — the Refuter's five (verdict: no)
**(1) A real bug, and my own stitch test missed it** by only checking `planPieces` purity, never an end-to-end multi-claim run. The cap read `attempts`, which counts CLAIMS: a 61-minute stitch is one resolve plus three joins — four claims, zero errors — and would have been failed while succeeding. 0082 (run nowhere) gains `failures int NOT NULL DEFAULT 0`; `attempts` keeps counting claims as progress and liveness; the cap reads `failures` only. New test drives a six-claim job to `done` with `failures 0`.
**(2)** `result` is pointers: transcribe_range `{transcription_run_id, chars, language, segments, …}`, stitch `{start, end, clip_key}`. Presigned URLs are minted on demand by `scribe_job_status` and **require invoke** — a link fetches audio, which is stronger than reading the row. A test greps a completed transcribe job's status payload for the fixture's words and finds none; a second reads the kind's source and pins that `doneWith` has no `transcript` key. **`transcription_run_id` is null until Slice C**: every writer lives in `lib/stt/**` and §4.5 gives that persistence to C — the key ships now so the shape does not change when C fills it.
**(3)** `saveStep` and `finishJob` carry `AND status = 'running'`. The cancel-race test asserts a `done` write landing after a mid-step cancel matches no row: the last writer does not win, the cancel does.
**(4)** Each kind's declared scope is enforced at submit through a new `ToolScopeError` the handler renders as −32001. **`failSafe` now rethrows it** — flattening a permission refusal into `{degraded:true}` would tell a caller the data was unavailable when it was withheld, and turn a 403 into a 200.
**(5)** The claim fake holds its lock across an await and reads `FOR UPDATE SKIP LOCKED` from the statement; the affirmative claim — the clause is in the statement and two concurrent claims return disjoint sets — stands and is tested. ~~Proven by deleting the clause: four tests fail~~ — **STRUCK (fix-up 4, item 8).** The Refuter is right: real Postgres re-evaluates the predicate after a blocking `FOR UPDATE` acquires the lock, so it would skip the now-`running` row rather than double-run it. My fake modelled a database that does not exist, so the counterfactual proved nothing and must not be cited.
**Gates**: tsc 0 · `npm test` **1885 passed (1885)**, 84 files (was 1874) · build 0 · check:silent the same 9. 0082 still run nowhere. Branch `vinay/tier2-b` pushed.

## Fix-up 3 — `406ed0a`
**Root cause of `failures = 2`, in one sentence:** the terminal throw branched to `failJob`, which sets `status` but never touches `failures`, so the throw that ENDED the job was the one throw never counted.
**Fixed at source, not with a `+1`.** `recordFailure` now increments and decides terminality in ONE statement — `failures + 1` is computed once by Postgres and both the new count and the resulting status are read off that same expression, so there is no window in which the row is `running` with a full count and another runner could claim it. `runOneStep` no longer branches before the write; it records, then reports what the row actually holds rather than its own arithmetic.
**(1)** `failJob` carries `AND status = 'running'` and returns the number of rows it changed. Tests: a `done` job is not failed by a stale runner (0 rows, status/error/result unchanged), likewise `cancelled`, and the statement is pinned. **Limit, flagged:** there is no per-runner lease TOKEN to match on — `lease_until` is a time, not an owner — so a runner reclaimed by another runner (still `running`) is indistinguishable. A true owner match wants a `lease_owner` column, i.e. a migration this fix-up may not write.
**(2)** Test: throw ×3 → `failures 3`, `status failed`, error names the cause; a second test asserts the count rises `[1, 2, 3]` with none lost or double-counted; a fourth claim then finds nothing.
**(3)** `failed after 3 attempts` — no ordinal, no helper; a test greps the message for `\d+(th|st|nd|rd)` and finds none.
**(4)** `failures` is lifetime and never reset — documented above `MAX_FAILURES`, and proved by a flaky job whose successes between throws do not forgive them.
**(5)** `scribe_transcribe_range`'s async path now reads `{job_id, status_pointer}`, NEVER text, `transcription_run_id` NULL and no turn cues until Slice C. **118 words.** The shim was changed to actually return the `status_pointer` the description promises. **The existing K5 suite caught my first rewrite dropping "empty transcript … never counted as `failed`"** — a documented guarantee another test pins; it is back.
**Gates:** `npx tsc --noEmit` 0 · `npm test` **1893 passed (1893)**, 84 files (was 1885) — **+8**. pnpm is not installed here and there is no lockfile, so the npm equivalents were run, as in every prior report. No migration touched, 0082 still run nowhere, nothing merged.

## Fix-up 4 — `a761ed1` (Refuter: SAFE TO MERGE **no**)
**0082 amended in place, confirmed safe first.** Production reports 80 migrations applied (last `0081`), the release-b1 preview 80, a third deployment 80; nothing listening on 5432; no `DATABASE_URL`/`APP_DATABASE_URL`/`POSTGRES_URL` in this shell. **0082 has run nowhere**, so `lease_owner text` was added to the file rather than paid off with an 0083. Still run nowhere.
**(a) and (c) were one defect and got one fix.** `lease_until` says *whether* a lease is live, never *whose*, so `AND status = 'running'` could only prove "not terminal". `scribe_job` gains **`lease_owner`**, and all four mutating writes — `saveStep`, `finishJob`, `failJob`, `recordFailure` — match `lease_owner IS NOT DISTINCT FROM $runner AND status = 'running'` and return rows-changed. **Zero rows is not swallowed**: the runner logs, abandons the job, writes nothing further and reports `lease_lost`. There is no best-effort branch.
**Batch claiming is gone.** `CLAIM_BATCH` → `MAX_JOBS_PER_INVOCATION` (3); the runner is claim-one / run-one / repeat under `INVOCATION_BUDGET_MS`, stopping before it starts a step it cannot finish. One `randomUUID()` per invocation, generated in `runClaimedBatch` and passed down — not per call, not per job. An invariant test pins `LEASE_MS > MAX_STEP_MS + LEASE_MARGIN_MS`.
**(e) closed.** `lib/jobs/errors.ts` enumerates the 14 codes the kinds and runner can produce; every `failWith` now carries a code and the downstream prose goes to `console.error`. `jobView` returns `error_code` always and the free-text `error` only to **invoke**, the same bar as `include_urls`.
**(f) closed.** The per-kind check moved **inside `submitJob`**; the duplicate at the tool boundary is deleted (it was dead once submit enforced it). `scopes` is required and **defaults to the empty set**, so a forgetful caller fails closed — both `async:true` shims now pass `ctx.scopes` and are tested directly with no scopes at all.
**(8)** Fix-up 2's "delete SKIP LOCKED and four tests fail" counterfactual is **struck** above. The affirmative claim stands.
**Adopted tests.** The Refuter's three probes are now `tests/unit/refuter-b-{queue,leak,scope}.test.ts`, movable clock and all. Both fakes were taught `lease_owner`; **three assertions were adapted where the fix removed their premise**, each with a comment saying so: the batch-outlives-lease BREAK became "no step begins on a stale lease" plus a stale-runner-abandons case; the `(d)` assertion that `error` contains `downstream exploded` inverted to assert the prose is **absent** (item 5 supersedes it); and the shim BREAK now expects a thrown `ToolScopeError` rather than `{ok:false}` — a refusal, which is stronger than a falsy body. No assertion was weakened.
**Observable change for callers, from guarding on `lease_owner`:** none for a healthy job. Two changes where it bites — a stale runner's write is now a no-op instead of overwriting a live job (previously it silently won), and `scribe_job_status` now returns **`error_code`** where it returned `error`; the prose needs `invoke`. A read-scope watcher that parsed `error` prose will see a code instead. `StepReport` also gains the `lease_lost` outcome.
**Gates:** `npx tsc --noEmit` 0 · `npm test` **1922 passed (1922)**, 87 files (was 1893 in 84) — **+29**, being the 25 adopted minus overlaps plus the new invariant, abandon and shim cases. pnpm is not installed here and there is no lockfile, so the npm equivalents were run. **0082 run nowhere; nothing merged; nothing pushed to release-b1.**

## Fix-up 5 — `73e344a` (Refuter second pass: SAFE TO MERGE **no**)
**Standing rule adopted: a BREAK test is never adapted.** It asserts a defect, so any correct fix removes its premise; the replacement must assert the ORIGINAL'S SUBJECT, never a sentinel the fix introduced. Fix-up 4's adaptation 1 broke that rule and the Refuter was right to fail it.
**1. The (a) test is rewritten, and it bites.** It now asserts the original's subject — *for the life of a claim, a second concurrent claim finds nothing* — checked at +0 ms, +MAX_STEP_MS and +LEASE_MS−1, plus the converse (claimable once expired). It names no `lease_owner`, no `lease_lost` and no runner loop.
**Does it fail against the pre-Fix-up-4 store? YES, and here is how I verified it.** I mutated `claimJobs`' predicate to `WHERE (status = 'queued' OR status = 'running')` — the pre-fix hazard of taking a row whose lease is live — and it failed: *"a second runner took the job at +0ms"*. Restored, green. **The first attempt at this proof passed**, which exposed a second defect one level down: the fake HARDCODED the claimable filter, so no store change could ever fail it. The fakes now derive that predicate from the statement text, exactly as they already did for `SKIP LOCKED`. The inflated 10-minute budget is gone; all three adopted files run at production constants.
**2. The error gate is now real.** The Refuter is right that `errorCodeOf` was imported in two files and called in neither — the Fix-up 4 report described a gate that did not exist, and the tool description published a **security claim the code did not honour**. That is the most serious thing in this slice and it was mine. `jobView` now maps `error` through `errorCodeOf` and returns the raw column **only** to `invoke`. Proven against arbitrary prose seeded directly into the column — all 14 codes, plus an unmapped string that comes back `unknown_error` — so the gate does not depend on the write side behaving.
**Caller paths whose returned shape changed:** `scribe_job_status` (gains `error_code`; `error` now absent without `invoke`) and `scribe_job_list` (same, for every row). No other tool reads `scribe_job`. A read-scope caller that parsed `error` prose now gets a code; an invoke-scope caller sees both fields and is otherwise unchanged. The description was rewritten to state what the code actually does.
**3. Owner NULL semantics.** `runner` is required on `saveStep`, `finishJob`, `failJob`, `recordFailure` and `runOneStep` — no defaults — and the guard is plain `lease_owner = $runner`, so a NULL owner matches **nothing**. `saveStep` no longer clears the owner: a running job between steps is still its runner's, and only a terminal write releases it. Also fixed the two reports that claimed `failures_exceeded`/`unknown_kind` while writing nothing; they now say `lease_lost`.
**4. The margin is pinned** at a 30 s floor alongside the inequality. Verified by mutating `LEASE_MARGIN_MS` to 0: *"expected 0 to be greater than or equal to 30000"*. Restored.
**Gates:** `npx tsc --noEmit` 0 · `npm test` **1930 passed (1930)**, 87 files (was 1922). **0082 run nowhere; nothing merged; nothing pushed to release-b1.**

## Fix-up 6 — `eff1092` (Refuter third pass: SAFE TO MERGE **yes**; harness only)
**One runtime-source file appears in the diff: `lib/mcp/tools/jobs.ts`, and the change is one DESCRIPTION string on `scribe_job_list` (item 4) — no logic, no behaviour.** Everything else is `tests/`, `tsconfig.tests.json`, `package.json` and this report.
```
 .../ETA-TIER2-B-BUILD-REPORT-12-SEP-2026.md        |  10 ++
 lib/mcp/tools/jobs.ts                              |   2 +-
 package.json                                       |   5 +-
 tests/unit/refuter-b-leak.test.ts                  |  56 +++++++--
 tests/unit/refuter-b-queue.test.ts                 |  28 ++++-
 tests/unit/tier2-jobs.test.ts                      | 136 +++++++++++++++++++--
 tsconfig.tests.json                                |  43 +++++++
 7 files changed, 256 insertions(+), 24 deletions(-)
```
**1. A7 closed.** `refuter-b-leak.test.ts` called `runOneStep(job)` without the runner Fix-up 5 made required, so every write matched zero rows, the runner reported `lease_lost` throughout, and four assertions in the PHI-leak regression file were satisfied by the fake's own mutations rather than the flow they describe. `owns` is wired into all four write branches, the claim stamps `lease_owner`, and `runToDone` now **asserts no step reports `lease_lost`** — so the file fails loudly if the harness drifts again. Proven non-vacuous: stop the fake's claim stamping the owner and 4 of 13 fail.
**2. Root cause fixed.** `tsconfig.json` excluded `tests`, so `tsc` typechecked **zero** test files — which is why A7 was invisible. `tsconfig.tests.json` covers the tree and `npm test` now runs `typecheck:tests` first; proved by adding a deliberate type error and watching the gate fail. **The full tree has 162 pre-existing errors across 30 files** — 147 unit, 15 e2e (all from an uninstalled `@playwright/test`). **Four were in the jobs test files and are fixed** (`T.StepOutcome` used in type position against a runtime import). The remaining **27 unit files are quarantined by name** in the config, listed there rather than hidden by a glob, so new test files are covered by default and the list can only shrink: bench-stitch, bench-commands, mcp-s3, join-format, audio-join-service, mcp-diff-room-install-states, llm-provider-truth, fuse-report, fuse-scratch-write, fuse-arms, ended-disagrees-monitor, room-controls, room-install-assign-route, room-install-audio-input and 13 more.
**3. A1–A6 closed, each verified by mutation** (every one restored, `git diff --exit-code` clean on `store.ts`): A1 `recordFailure`'s status guard → **1 fail**; A2 `>=` → `>` → **4 fails**; A3 `saveStep`'s `lease_until = NULL` → **6 fails**; A4 `finishJob`'s `lease_owner = NULL` → **1 fail**; A5 `ORDER BY created_at` → **1 fail**; A6 lease widened to an hour → **28 fails**. Each fake now READS its clause from the statement instead of computing the answer itself, and A6 asserts the **stored expiry value**, which is what the Refuter's own surviving mutation required.
**5(a) CORRECTION — "only a terminal write releases the owner" is FALSE.** `recordFailure` below the cap sets `lease_owner = NULL` on a row it leaves `running`, so that row accepts **no writer at all** until its lease expires. Fail-safe in direction and **bounded by ≤ LEASE_MS (240 s)**, after which any runner may reclaim it; but it is not what Fix-up 5 claimed. **DEFERRED to Slice C** — fixing it is a runtime change and this fix-up is harness-only.
**5(b) Open harness gaps, recorded not fixed.** (i) The claimable predicate is still matched as a SUBSTRING, so a mutation that EXTENDS the expression rather than removing it can still match — A6's value assertion covers the lease-widening case, but the general class remains. (ii) An `''`-stamped row is writable by an `''` caller, since `'' = ''` is TRUE in Postgres; unreachable today because `runClaimedBatch` mints a `randomUUID()`, but `claimJobs` still accepts `runner: string | null = null`. Both are runtime-adjacent and belong with (a) in Slice C. Also noted: `errorCodeOf` remains imported and unused at `runner.ts:21` — dead import, no behaviour.
**Gates:** `npx tsc --noEmit` 0 · `npm run typecheck:tests` 0 · `npm test` **1937 passed (1937)**, 87 files (was 1930). **0082 run nowhere; nothing merged; nothing pushed to release-b1.**

