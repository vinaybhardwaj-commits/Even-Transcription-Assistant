# ETA — Open Items / Incomplete Work Tracker

**Living document.** Everything started-but-not-finished, planned-but-not-started, and pending-V-action across the ETA (Evenscribe) project. Scope: ETA only — say the word if you want this broadened to Even OS / EPI / OPD-Encounter-App.

**Last updated:** 6 Jun 2026 · **Repo HEAD:** `4d750c6` · **📌 AUTHORITATIVE HANDOFF: `ETA-CARRYOVER-PROMPT-2-JUN-2026.md`** (paste into a new thread; supersedes the 31-May one — pair with the 6-Jun block below). Migrations applied: **0001–0026** (0013 skipped). All 6 services green; `npm run smoke` 9/9.

**Shipped 6 Jun (later — Dr Ankit field bugs, B22, HEAD `87a580d`):** (a) **Live `http_413 FUNCTION_PAYLOAD_TOO_LARGE` FIXED** — `use-sarvam-rolling.ts` was sending the whole uncommitted span to the Vercel serverless fn (>4.5MB cap) and the watermark only advanced on non-empty tail+≥88 chunks, so silence / iOS ~1s chunks grew the window past the cap and wedged every later tick. Now bounded by bytes (3.5MB, slide effStart) + commit on chunk-count OR forced size-advance (not gated on tail). (b) **SW `FetchEvent.respondWith ... Load failed` FIXED** — `sw.js` static branch `.catch(()=>cached)` returned `undefined` on cache-miss+offline; now `cached || Response.error()`, SHELL_CACHE v2→v3. Live `52bb906`, smoke 9/9, CI green, /sw.js served v3. **PENDING-V: Dr Ankit device retest (must reload once for SW v3).**

**Shipped 6 Jun (this session):** (1) **Tier-4 flag #17 ACTIVATED** — `NEXT_PUBLIC_ETA_TRIM_LIVE_BUFFERS=1` set in Vercel + baked live; **PENDING-V device test** (`ETA-TIER4-FLAGS-DEVICE-TEST.md`), then #18/#19 one at a time. (2) **CI fixed — it had NEVER run** (B21 in the bug log): failed at Setup Node on every run ever (repo has no `package-lock.json`; `cache:"npm"` + `npm ci` both require one), then configless `next lint` + bare-npx vitest failures surfaced; 3 commits (`6bb3deb`/`c6ff5fa`/`6fcd2c8`) → **CI green end-to-end for the first time** (typecheck → vitest → check:silent). (3) **mic-denied e2e first CI run: 3/3 passed.** (4) **eka.care credits RESOLVED** (`txn_limit_exceeded` gone; auth+presigned+full scribe job verified) — then **V decided to RETIRE EkaScribe (too expensive)**: migration **0026** disables the row (adapter/API/row kept, reversible). (5) **NEW scribe-tier engine `elevenlabs_scribe`** (ElevenLabs Scribe v2 ASR → same Even note-gen LLM → rubric vs even_pipeline; isolates ASR as the only variable) — **fanned out across ALL encounters with note+audio** (2 junk/silent clips errored `asr: empty_transcript`, by design); `runScribeForEncounter` passes `note_type` as template; new worker mode `{scribe:true, missing:true}` (errored attempts count as attempted — no B17-style loop). View: `/admin/stt-lab` scribe leaderboard.

**Shipped 2 Jun (this session) — full detail in `ETA-CARRYOVER-PROMPT-2-JUN-2026.md` §4.B + `ETA-BACKLOG-SCOPED.md`:** (1) **Reliability backlog COMPLETE** — all 20 non-security items, Tiers 1–5 (crypto PINs · lazy DB URL · voice_sample uniq idx [0024] · STT scoring; Sarvam timeouts · Whisper RMW · Deepgram header · preflight guard · RecoveryModal sentinel · mic-denied e2e; scribePending loop · STT budget enforce · **drainFanout atomic claim** [0025] · dup-email guard · Resend webhook hardening · **stuck-processing reaper** + hourly Vercel cron; Tier-4 flag-gated trim/reconnect/Safari-guard; swallow-handler triage + `check:silent` CI gate). (2) **Testing harness**: `npm run smoke` (daily canary) + `check:silent` CI gate + vitest + **Playwright e2e GREEN** (B18 regression). (3) **New admin surfaces**: `/buglog` (auth-gated; repo `content/ETA-BUG-LOG.md` is now CANONICAL → edit + push republishes), **encounter Audio play/download tab**, **Admins management** (add admin + reset password; unauth `seed-team` RETIRED), **System Map** module.

**Still OPEN (6 Jun):** **SECURITY (parked by V):** RBAC role gates · finalize-upload key-binding · admin-login lockout · rotate the shared admin password (`<REDACTED>`). **Pending-V:** **#17 device test** (then Claude rolls #18/#19); add Aditya's admin account (via `/admin/admins`); Dr-Ankit iPhone B18 retest; STT gold labels + per-engine cost_per_min; voiceprint passive smoke; physiotherapist pilot. ~~mic-denied e2e first CI run~~ ✅ 6 Jun (3/3). ~~eka.care credit link~~ ✅ resolved 6 Jun, then EkaScribe retired per V (row disabled, code kept). **Offered, not built:** remove-admin control; System-Map stage drill-down; doctor-side /buglog.

---

**(historical header below — superseded by the 2-Jun block above; kept for context)**
**Prior:** 1 Jun 2026 · **Repo HEAD:** `4725453` · **📌 AUTHORITATIVE HANDOFF: `ETA-CARRYOVER-PROMPT-31-MAY-2026.md`** (paste into a new thread). · **🎉 V2.0 COMPLETE (S0–S8)** + **🧪 STT ENGINE LAB COMPLETE (L0–L7)** + **🎙️ VOICEPRINT RETENTION A+B LIVE.** 5 note types × 3 clinician types; `clinician` is the sole identity table (`doctor` dropped, 0015). v1 + multilingual + diarization + streaming all live. Migrations applied: **0001–0022** (0013 skipped). All 6 services green on `d815c20`.

**Shipped since the last header (31 May):** (1) HOTFIX `43063e7` — 6 `.tsx` `FROM doctor` reads survived the S8 drop → 500s; switched to `clinician`. (2) `admin-pin-visibility-shipped` @ `750e5e4` — super-admins see + auto-refresh each clinician's 4-digit PIN (`clinician.pin_plaintext`, migration `0016`). (3) **Voiceprint retention A** `0d0cefd` + **B (passive capture, app+Mini) LIVE** `f72e3d7` — `voice_sample` table (0017), retain audio+embeddings, accumulate, retrain, download, delete, passive capture at conf ≥ 0.82. (4) **STT Engine Lab L0–L7 COMPLETE** `stt-lab-complete` @ `b70a280` + polish `d815c20` — registry+adapters+health · fan-out+backfill · agreement+judge · gold WER/term · composite dashboard · stage×lang routing · ElevenLabs Scribe v2 (L6) · Ekascribe + 2-tier scribe (L7); 5 ASR engines + scribe tier; migrations 0018–0022.

**Shipped 1 Jun (this thread):** **STT Lab — EkaScribe FIXED + scribe leaderboard populated** (migration 0023, `9e66276`+`4725453`). EkaScribe was producing zero data — diagnosed (B17) as: enabled after the backfill (never fanned out) + `transcript_template` silently unsupported on our account (ASR `no_output_for_template`) + `clinical_notes_template` requested alone → `template_failure` (only resolves when `eka_emr_template` is co-requested). **Fix = scribe-only** (dropped `asr` capability) + scribe co-requests `eka_emr_template`. Scribe board now: EkaScribe **ok 6 / judge avg 6.33** (3–9) vs Even pipeline (reference). Added targeted scribe trigger `POST run-fanout {scribe:true, encounterId}`.

**Remaining (all pending-V, no code blocked):** **🔴👤 eka.care free session quota EXHAUSTED (`txn_limit_exceeded`)** — top up / upgrade the eka.care plan to finish fanning EkaScribe scribe across the remaining clinical encounters (mirror of the ElevenLabs Rs.1000 top-up); the EkaScribe-vs-Even fight only becomes fair once clinicians **edit** notes (`note_json_edited` = gold, since even_pipeline currently scores ~10 against itself). STT Lab — label gold (activates accuracy/WER) + set per-engine cost_per_min (cost scoring); voiceprint passive-capture real-encounter smoke test; physiotherapist pilot name/provisioning; device tests of all 5 note types + B14/streaming; cosmetic (schema.ts stale `doctor` pgTable, add `OPENAI_API_KEY` for cloud term extraction, re-add 2 STT-Lab secrets as Sensitive). **Found bug (unfixed):** blind `scribePending` infinite-loops on junk clips with empty `note_json` (`scoreScribe` sets no `scored_at` done-marker) — use the targeted trigger until fixed.

Legend: ⬜ not started · 🟦 in progress · ✅ done (kept briefly for context) · 🔴 blocker · 👤 needs V

---

## 0. NEW (30 May session) — what's live + what's open

**Shipped + live this session** (see `ETA-NEXT-THREAD-BOOT-PROMPT.md` for the full tag table): multilingual Sarvam (codemix live + batch translate), submit-time speaker diarization (Mac Mini pyannote) surfaced to admin **and** doctor (speaker summary + tagged conversation), voice enrollment (wizard recording-evidence + admin kiosk), note speaker-tagging (English via Deepgram + non-English via Sarvam), first-person→Patient role override, EER harness `/admin/diarization`, English-translation + vernacular transcript boxes, live latency rework A (REST refine ~2-3s) **and** B (real-time Sarvam streaming WS via Mac Mini relay), live language **auto-detect→lock**, plus the `no_token` cookie-path fix + streaming→REST fallback + refresh-on-stream-close. HEAD `fa41130`.

**Open bugs / polish (do next):**

| Item | Status | Notes |
|---|---|---|
| **B14 — opening ASR hallucination / ad-bleed (no no-speech guard)** | ✅ CLOSED | `enc_g754qhq7z8` Marathi test: "English translation" box headed by fluent English garbage (Orient fan ad + filler). Diagnosed: NOT a translation failure — audio was a played TV serial with an English ad intro. **App-side** guard shipped + verified (tag `b14-transcript-guard-shipped` @ `eeaad21`): `lib/transcript-guard.ts` leading-foreign trim + bounded leading hallucination/ad strip in `/process`, length-floored. **Source-side** applied by V on the Mac Mini (`uk.llmvinayminihome.whisper` + `--vad`/`--no-speech-thold 0.7`/`--suppress-nst`); sandbox-confirmed silence→`{"text":""}`. Only follow-up: V device re-test with a clean Marathi consult (confidence check). See `ETA-BUG-LOG.md` §B14, `ETA-MAC-MINI-BACKEND-HANDOVER.md` §13. |
| Doctor self-serve enrollment live-text 401s | ✅ SHIPPED | tag `small-bugs-batch-shipped` @ `1839d0b`. New slug-scoped `/{slug}/api/voice/transcribe-window` (doctor-authed, verifies `claims.slug===slug`); doctor wizard repointed at it so the path-scoped `rounds_session` cookie is sent. Admin kiosk keeps the bare route (admin cookie is global). Route deployed + auth-verified (401 no-cookie). **Pending:** V device test — doctor self-serve wizard now shows live English text. |
| Streaming auto-reconnect | ✅ SHIPPED | `1839d0b`. `useSarvamStreaming` now reconnects on an unexpected WS drop with exponential backoff (0.5→8s, max 5), preserving the audio graph + accumulated transcript + locked language; after max retries → error → REST fallback. Initial-connect failures still fall back fast (`everOpened` gate); lock-swap closes flagged intentional. |
| Language-lock neighbor mislock | ✅ SHIPPED | `1839d0b`. Count vote replaced with a word-count-weighted vote + runner-up margin (leader must beat runner-up by ≥1.5×+2 with ≥12 weight, else timeout-pick after 10 utterances) — a long clear utterance outweighs short hi-vs-mr blips. |
| 65s clip → 1 speaker | ⬜ data-gated | pyannote under-segmentation on short audio (OSD/diar tuning, V2.SD.6). |
| Voices-heard-in-last-10s live counter | ❌ declined | True live speaker count needs per-window embeddings+clustering (Mac Mini load + unreliable on short windows). Real count = post-submit breakdown. |
| **Post-drop 500s — 6 `.tsx` page doctor-reads survived S8** | ✅ SHIPPED | HOTFIX `43063e7`. The S8 read sweep used `grep --include=*.ts`, which EXCLUDED `.tsx`, so six page-level `FROM/JOIN doctor` reads survived the `DROP TABLE doctor` → 500s: `app/[slug]/record` (CRITICAL), `app/[slug]/encounter/[id]`, `app/[slug]/recipients`, `app/[slug]/onboarding/voice`, `app/admin/doctors/[id]/voice` (V hit this), `app/encounter/[id]`. All switched to `clinician`. Verified live: voice kiosk + clinician detail + list all **200**. |
| **Voiceprint sample retention + retrain + download (Sprint A)** | ✅ SHIPPED | tag `voiceprint-retention-sprintA-shipped` @ `0d0cefd`. V wants every clinician voice sample RETAINED + downloadable (audio AND embedding) + a "retrain" using all samples over time. Pre-31-May the raw audio was discarded (only the averaged centroid + per-clip embeddings in `voice_print.samples_json` were kept) and re-enroll overwrote. Migration `0017` new `voice_sample` table (per-clip embedding + R2 audio key + source enrollment\|passive); backfilled existing embeddings. Both enroll routes now retain audio to R2 + accumulate (recompute centroid from ALL samples). New admin endpoints (list / audio dl / embedding dl / centroid dl / delete / retrain) + a "Voice samples" panel on the clinician detail page. All verified live. Build doc `ETA-VOICEPRINT-RETENTION-PRD.md`. |
| **STT Engine Lab (test-bed)** | ✅ COMPLETE L0–L7 (`stt-lab-complete` @ `b70a280`) | Registry+adapters+health · fan-out+backfill · agreement+judge · gold WER/term · composite dashboard · stage×lang routing · **L6 ElevenLabs Scribe v2** · **L7 Ekascribe (async job adapter, verified) + scribe tier (audio→note vs Even pipeline, qwen rubric vs clinician note) + even_pipeline competitor.** 5 ASR engines + 2-tier scribe. **Operating: label gold to activate accuracy; set cost_per_min in Engines tab; scribe needs real consults; add OPENAI_API_KEY for cloud term extraction.** | L0–L5 core + **L6: ElevenLabs Scribe v2 added as a 4th competitor (adapter + enable row → auto-joined everything, no schema/UI change = plug-in path proven). Fanned out all 25; leaderboard deepgram 61 / sarvam 48 / whisper 44 / elevenlabs 38 (no gold yet).** worker `{dedup}` added. **L7 next: Ekascribe + scribe tier (audio→note vs Even pipeline).** | Observation (L0–L4) + control (L5 routing) done. L5: `stt_routing` table + resolver + Routing tab; note/English enforced server-side (finalize-upload), live advisory, diarize single-engine. Safe/reversible — defaults to current behaviour. **L6 next: add ElevenLabs Scribe v2 as a competitor engine (keys live in Vercel); L7: Ekascribe + scribe tier.** | L0 registry/health · L1 fan-out · L2 agreement+judge · L3 gold WER/term · **L4 dashboard: composite Leaderboard (configurable weights, lang/date filters) + Runs browser (side-by-side transcripts + gold word-diff + scores) + Engines tab (enable/fanout/cost). Observation half (L0–L4) COMPLETE.** **L5 next: stage×language routing (the control half — wires production paths to read a routing table, behind safe fallback).** | L0 registry/health · L1 fan-out+backfill · L2 agreement+judge · **L3 gold set: verbatim-reference labeling UI + WER/CER + medical-term fidelity (migration 0020 stt_gold); cloud-or-qwen term extraction; Gold set tab w/ per-engine WER leaderboard + label/remove.** Verified WER math live. **Add `OPENAI_API_KEY` in Vercel to use cloud term extraction. L4 next: composite-score dashboard (Leaderboard + Runs tabs).** | L0 registry/adapters/health · L1 fan-out+backfill (25 enc, deepgram/sarvam/whisper) · L2 reference-free scoring: agreement (token-similarity) + blinded qwen judge → judge_score/agreement_score/is_winner; all 25 scored (sarvam leads on the test set). qwen.ts now falls back to OLLAMA_BASE_URL. **L3 next: gold set + WER/CER + medical-term fidelity.** | L0 registry+adapters+health. L1 offline fan-out: queue + worker + `/process` after() hook + backfill of all 25 audio encounters (deepgram 25/25, sarvam 25/25 via batch, whisper 21/25; 71/75 ok). Sarvam sync 30s-cap → adapter uses batch for long-form. transcription_run extended (tier/cost/wer/.../note cols). **L2 next: agreement + N-engine LLM judge scoring on every fan-out.** | `ETA-STT-ENGINE-LAB-PRD.md`. Compare ALL STT engines on accuracy/speed/cost/reliability to pick the best medical stack. V's decisions: 4 accuracy methods; auto fan-out every encounter ($5/day cap); stage×language routing; hybrid judge (Mini qwen + cloud gold-term extraction); verbatim ASR gold; backfill all 34 enc. **2 new competitor engines:** ElevenLabs Scribe v2 (L6) + Ekascribe (L7); Google Chirp 3 dropped (no Google Cloud STT). **TWO tiers:** ASR (transcript→WER) + Scribe (audio→note, Ekascribe vs Even pipeline, scored vs clinician note). Registry+adapter (async-capable) = new engine is 1 file + 1 row. Plan L0–L7. **Keys OBTAINED 31 May (ElevenLabs + Ekascribe) → in `_sprint0-secrets/eta-vercel-env-vars.env` (`ELEVENLABS_API_KEY`, `EKACARE_CLIENT_ID`, `EKACARE_CLIENT_SECRET`); set in Vercel before L6/L7. No external blockers. Awaiting go-ahead to build L0.** |
| **Voiceprint passive capture (Sprint B)** | ✅ LIVE (app + Mini) | App hook `voiceprint-retention-sprintB-apphook-shipped` @ `f72e3d7` + Mac Mini `/diarize` change applied & verified 31 May (`server.py` sha `a2f1f643…`; sandbox re-verified `embedding_base64` = raw float32[192], L2 norm 311.62). `/process` now appends a passive `voice_sample` for the matched doctor (embedding + encounter-audio ref) at confidence ≥ `PASSIVE_VOICEPRINT_GATE` (0.82); below-gate retained+downloadable but not averaged. **Remaining: V's real-encounter app-side smoke test** (record as a matched clinician → passive sample appears in the admin Voice samples panel). |
| **Admin PIN visibility for super-admins** | ✅ SHIPPED | tag `admin-pin-visibility-shipped` @ `750e5e4`. V's ask: super-admins should see + auto-update the PIN per clinician. New `clinician.pin_plaintext` (migration `0016`), populated on create/reset/bootstrap; detail page shows "Current PIN: NNNN" (mono), gated to role=super (ops/viewer get null); reset already calls `load()` so it refreshes to the latest. Existing rows show "reset to reveal" until their next reset (bcrypt is one-way — no historical plaintext). Verified live: dr-vinay was null → reset to **6110** → `pin_plaintext='6110'`, `pin_set_at` updated. **Build note:** first push `7fc0169` ERRORED (duplicate `import { sql }`), prod safely held; fixed `750e5e4`. |

**Pending V (device tests):** 5-min Marathi (language settles + holds; speaker summary auto-shows), full kiosk enrollment of another doctor, optional relay `127.0.0.1` bind hardening.

---

## 1. Multilingual transcription & multi-engine testbed (NEW — PRD locked-in-progress)

PRD: `ETA-MULTILINGUAL-TRANSCRIPTION-PRD.md`. Fixes the Kannada failure (blank live panel + Kannada note); builds the stacked-engine comparison testbed.

| Item | Status | Notes |
|---|---|---|
| Sarvam API key | ✅ | Provided 29 May; in `_sprint0-secrets/eta-vercel-env-vars.env` AND set in **Vercel env (Production+Preview, Sensitive)** + redeployed (sha `b5791b6`) via Chrome browser control. |
| Lock D1-D8 + §10 open questions | ✅ | All locked 29 May. Headline: Sarvam = primary live engine (auto-detect), English note, original preserved, judge-winner→note, new `transcription_run` table, first ship = MT.0+MT.1+MT.2. |
| MT.0 — schema `0006` (`transcription_run`, `encounter.detected_language` + `transcript_original`) | ✅ committed | Migration written + `schema.ts` patched + pushed to main (`0006`). NOT yet applied to Neon — run `/api/run-migrations` at ship. |
| `lib/sarvam.ts` foundation | ✅ committed | `sarvamTranscribe` / `sarvamTranslate` (saaras:v3, webm-direct, `api-subscription-key` header) + `isNonEnglish`. Validated live on a real Kannada clip. |
| MT.1 — submit-time English note (non-English → Sarvam translate → note; English path untouched) | ✅ SHIPPED | `finalize-upload` now overrides to Sarvam English for non-English (detected via the rolling lang); stores `transcript_original` + `detected_language`; note-gen prompt has an English backstop. Length handled by the rolling-accumulation approach (no Batch API needed). |
| MT.2 — near-live rolling REST panel (Deepgram-first; switch to Sarvam window text when non-English) | ✅ SHIPPED | Route `/api/transcribe/sarvam-live` (parallel transcribe+translate) + `useSarvamRolling` hook (≤30s windows, header-prepended) + `SarvamTranscript` panel in RecordingScreen. English encounters keep Deepgram untouched. |
| **Shipped** | ✅ | Tag `mt-multilingual-v1-shipped` @ `8a38746`. Migration `0006` applied to Neon. All 6 services healthy. **Needs V device smoke-test** (record Kannada → native script live → submit → English note). |
| ~~MT.3 — admin Engines comparison view~~ | ✅ SHIPPED | tag `mt3-engines-view-shipped` @ `3463b77`. New "Engines" tab on the admin encounter detail shows per-engine (Deepgram/Whisper/Sarvam) original + English + latency + winner badge, plus detected language + the live code-mixed transcript. Reads `transcription_run`. Multilingual testbed now closed out. |
| MT.4 — local streaming engine (WhisperLive / AI4Bharat IndicConformer) as 4th engine | ⬜ 👤 | Needs Mac Mini + tunnel (V away from box) |

---

## 2. Speaker diarization (v2.1) — service BUILT, not reachable from Vercel

Docs: `ETA-DIARIZE-SERVICE-HANDOVER.md`, `ETA-DIARIZE-PHASE-7-8-REMOTE-RUNBOOK.md`, `ETA-V2.1-MAC-MINI-PYANNOTE-SETUP.md`, `ETA-V2-PRD.md` §20.

| Item | Status | Notes |
|---|---|---|
| HuggingFace pyannote registration + token | ✅ | Done; token at `~/.huggingface/token` on Mac Mini |
| Diarize service built + locally smoke-tested | ✅ | `~/eta-diarize/server.py`, `/diarize` contract confirmed on `.wav` |
| ~~Phase 7 — launchd auto-start~~ | ✅ DONE | `uk.llmvinayminihome.eta-diarize`, survives reboot. |
| ~~Phase 8 — Cloudflare tunnel~~ | ✅ DONE | `https://diarize.llmvinayminihome.uk` live (tunnel `llm-tunnel`, `/etc/cloudflared/config.yml`). `/health` 200 over HTTPS. |
| ~~Phase 9 — external webm smoke test~~ | ✅ DONE | Real webm → 200, full contract, ~4.7s. **webm decode risk CLOSED.** Re-verified from Evenscribe sandbox off-network (~1.3s). Fixed a launchd-PATH/ffmpeg bug. |
| ~~Phase 10 — Vercel env wiring~~ | ✅ DONE | `DIARIZE_BASE_URL=https://diarize.llmvinayminihome.uk` + `DIARIZE_TIMEOUT_MS=90000` set in Vercel (Production+Preview) + secrets file. Inert until V2.SD.3 reads them. |
| ~~V2.SD.6 — EER measurement harness~~ | ✅ SHIPPED | tag `v2sd6-eer-harness-shipped` @ `5c2bfb7`. Admin **Diarization** page (`/admin/diarization`) lists clinician auto-matches + per-encounter ✓/✗ labeling; `/api/admin/diarization-eer` computes EER once ≥3 correct + ≥3 incorrect labels exist (migration `0008` identification_label). Tuning the 0.70/0.78 thresholds waits for ~50 pilot encounters; this collects the ground truth. Currently 0 matches (no real clinician-identified encounter yet). |
| ~~V2.SD — note speaker-tagging~~ | ✅ SHIPPED | tag `v2sd-note-tagging-shipped` @ `fc8af8e`. Sarvam batch translate now runs `with_diarization=true` → timed **English** segments w/ anonymous speaker_id; `reconcileTagged()` (lib/diarize.ts) maps those onto pyannote's **named** speakers by time-overlap; `/process` stores `tagged_transcript` (migration `0009`); admin Speakers tab renders the speaker-tagged English conversation. Non-English path only (Sarvam); English speaker-tagging via Deepgram utterances = future. Populates on next non-English encounter. |
| ~~V2.SD.0 — diarization schema (migration 0007)~~ | ✅ DONE | tag `v2sd0-schema-shipped` @ `a60011b`. `voice_print` table + encounter diarize columns + `doctor.email_show_conversation_with` applied to Neon (adapted to `doctor`, not the not-yet-existing `clinician`). |
| `/enroll` endpoint on diarize service | 🟦 👤 | Code + runbook ready: `ETA-DIARIZE-ENROLL-ENDPOINT-RUNBOOK.md`. **V applies on the Mac Mini** (Cowork session, reuse the loaded ECAPA model) → then sandbox verifies over the tunnel. |
| ~~V2.SD.1 — voice enrollment wizard~~ | ✅ SHIPPED (code) | tag `v2sd1-enrollment-shipped` @ `c98f281`. `/{slug}/onboarding/voice` 6-sentence wizard + `/api/voice/enroll` (Mac Mini `/enroll` ×6 → averaged centroid → `voice_print`) + HomeShell "Set up voice recognition" CTA. **Soft gating** (/record stays open). Build green, routes verified live. **Pending end-to-end: V applies the `/enroll` runbook on the Mac Mini**, then sandbox verifies a real enrollment. Deferred: historical-bootstrap (SD-Q6), super-admin skip flag, re-enroll-from-settings. |
| **V2.SD.2 — live speaker badges** | ⬜ | Deepgram diarize=true + speaker pills on the live transcript + tap-to-relabel. |
| ~~V2.SD.3 — submit-time pipeline (core)~~ | ✅ SHIPPED | tag `v2sd3-diarize-integration-shipped` @ `0b2b477`. `/process` calls live `/diarize` (soft-fail, status lifecycle), stores `speakers/transcript_segments/overlap_windows/aggregates/diarize_*`; admin **Speakers tab** renders it. Verified in Vercel runtime (5.5s, stored). **Deferred to follow-ups:** word-level speaker-tagged note text (needs Whisper word-timestamp alignment), the "Conversation with:" email line (needs the SD-Q4 toggle UI), and clinician naming (needs enrollment). Runs with heuristic roles until centroids exist. |
| ~~V2.SD.5 (v1) — admin Speakers timeline~~ | ✅ SHIPPED | tag `v2sd5-speakers-timeline-shipped` @ `f60c940`. Gantt of who-spoke-when (per-speaker color rows over `transcript_segments`) + overlap markers, on the Speakers tab. **Deferred:** audio playback (needs a presigned-GET route) + super-admin re-run button. |
| ~~V2.SD.2 — live speaker pill~~ | ✅ SHIPPED | tag `v2sd2-live-pill-shipped` @ `6afa8ca`. Live "Speakers" pill above the codemix box: "Listening for your voice…" → "Identified · Dr X · NN%". `/api/voice/identify` (rolling `/enroll` embed + cosine vs the doctor's `voice_print` centroid, 0.78 live threshold) + `useSpeakerIdentify` hook. Verified: self-match 0.997, different voice correctly not matched. **V to device-test (record your own voice → pill names you).** Tap-to-relabel deferred (needs live multi-speaker labels). |
| Note speaker-tagging | ⬜ | DECISION was Sarvam word-timestamps → align to pyannote. **Finding (30 May): Sarvam SYNC has no diarization/timestamps** → must use the Sarvam BATCH `with_diarization` (timed speaker-tagged English segments) + reconcile Sarvam's anonymous speaker_id with pyannote's NAMED clinician by time-overlap. Clean but multi-step. Next build. |
| V2.SD.6 — threshold/EER tuning | ⬜ | EER measurement harness scaffolded next (admin stats + correct/incorrect labels); actual tuning needs ~50 pilot encounters. |
| ~~Apply `/enroll` on Mac Mini~~ | ✅ DONE | V's Mac Mini session added `POST /enroll` (reuses the loaded ECAPA model). Live + verified over the tunnel: ok/dim 192/768 bytes. |
| ~~Enrollment → naming loop~~ | ✅ VERIFIED (tag `v2sd-enrollment-naming-verified` @ `f97e7ae`) | App `/api/voice/enroll` writes `voice_print` (Mac Mini `/enroll` ×N → avg centroid); `/process` loads the doctor's centroid + passes to `/diarize`. Direct self-match test = **0.997 clinician/auto**; different voices correctly NOT matched. |
| **👤 Re-enroll with your real voice** | ⬜ 👤 | The sandbox test wrote a `voice_print` for dr-vinay from a *cookbook sample voice* (placeholder), so the HomeShell CTA is now hidden. To get named in your own encounters, open `/dr-vinay-bhardwaj-cjzs/onboarding/voice` directly and record your 6 sentences (overwrites). |
| PRD §20 + setup runbook version pins are STALE | ⬜ | Reconcile to actual: pyannote 3.3.2, torch 2.2.2, numpy<2, hf_hub 0.23.4, `PYTORCH_ENABLE_MPS_FALLBACK=1` |

---

## 3. v2.0 — note types × clinician types (PRD locked, NOT started)

PRD: `ETA-V2-PRD.md` §3 (11 decisions + D5 locked). 5 note types × 3 clinician types.

| Item | Status | Notes |
|---|---|---|
| V2.S0 — schema foundation | ✅ SHIPPED | tag `v2-s0-schema-shipped` @ `ded9134`. Migrations **`0010`** (clinician table — copied from doctor, ids preserved, type='physician') + **`0011`** (encounter.note_type) applied to Neon, idempotent, additive (doctor stays read path). schema.ts updated. `recipient_per_doctor` rename + `doctor` drop deferred to V2.S6/S8 (no repoint needed in S0 since clinician.id=doctor.id). `kb_chunks.source/discipline` deferred (KB DB, not load-bearing for v2.0). See `ETA-V2-PRD-RESCOPE.md`. |
| V2.S1 — clinician model dual-write | ✅ SHIPPED | tag `v2-s1-dual-write-shipped` @ `da5a976`. `lib/clinician.ts syncClinicianFromDoctor()` (idempotent upsert, soft-fail) called after every admin doctor mutation (create/PATCH/reset-pin/rotate-url); admin doctors list shows read-only `clinician_type` (LEFT JOIN clinician). doctor stays read/auth path. Verified: all 10 doctors join to `clinician_type=physician`, PATCH 200, no sync soft-fail in logs. Doctor-self-serve PIN sets + last_active caught by the V2.S6 Phase-C backfill. |
| V2.S2 — note-type infra + Clinic + General Medical | ✅ SHIPPED | tag `v2-s2-clinic-general-shipped` @ `c2cbd8e`, build green, clinic path verified intact. **S2a**: note_type picker (HomeShell) → create stores it (physician allow-list) → `/process` passes noteType. **S2b**: distinct GeneralMedicalNote schema (reason_for_visit/active_problems/interval_history/impression/plan{investigations_ordered,treatment_changes,consultations_requested,follow_up}) across `generateNote` (GM prompt+shaping), NoteView (`GeneralMedicalView`), NoteEditor (`GeneralMedicalEditor`), email (GM html+text, type-first subject O4), CDMSS (`noteToSeedQuery`+stub branch), send/resend (`noteHasContent`), `/note` save (note-type-aware shaping), list titles (COALESCE reason_for_visit). Clinic untouched (branch-by-note_type w/ local casts). Build fix `c2cbd8e` (runCdmssStub→AnyNote). **Pending V device test:** record a General Medical note → distinct sections render + email subject `[General Medical]`. |
| V2.S3 — Operative/Procedure Note | ✅ SHIPPED | tag `v2-s3-operative-shipped` @ `8505346`, build green, clinic+GM verified intact. Distinct `OperativeProcedureNote` (21 fields incl. specimens[]/implants[] object arrays, numeric EBL/urine, tri-state counts_correct) across `generateNote` (SYSTEM_OPERATIVE + shaping), `OperativeView`, `OperativeEditor` (strings/lists editable; numerics/specimens/implants preserved + round-tripped), email (op sections + `[Operative Note]` subject), `/note` save. **CDMSS-OFF routing**: new `noteTypeHasCdmss()` gate in `/process` (both paths) — operative skips CDMSS, status→complete, cdmss_json null. 3-way picker. **Recipient defaults (OR/anesthesia/pathology) deferred to V2.S7** (per-note-type recipient defaults). **Pending V device test:** record an op dictation → op sections render, no CDS card, subject `[Operative Note]`. |
| V2.S4 — Dietetic Consult (pilot: Afshan Kamar) | ✅ SHIPPED | tag `v2-s4-dietetic-shipped` @ `1663ac6`, build green, clinic/GM/op verified intact. `DieteticConsultNote` (nested anthropometrics + diet_plan) across generate (SYSTEM_DIETETIC + shaping)/`DieteticView`/`DieteticEditor`/email (`[Dietetic Consult]` subject)/`/note` save; CDMSS off (noteTypeHasCdmss). **First non-physician clinician_type**: `syncClinicianFromDoctor(id, type)` sets it on create; admin CreateDoctorModal has a Physician/Dietitian/Physiotherapist selector; doctor home resolves clinician_type → **clinician-type-aware picker** (dietitian → Dietetic Consult only). **Pilot dietitian Afshan Kamar provisioned**: `/dr-afshan-kamar-kn4c` (kept `/dr-` per O5), PIN reset available in admin. **Pending V device test:** log in as Afshan → record a dietetic consult → anthropometrics + diet-plan sections render, subject `[Dietetic Consult]`. |
| V2.S5 — Physiotherapy Note | ✅ SHIPPED | tag `v2-s5-physio-shipped` @ `152f66a`, build green. `PhysiotherapyNote` (nested pain_assessment + treatment_plan, numeric pain score/sessions/duration) across generate (SYSTEM_PHYSIO + shaping)/`PhysioView`/`PhysioEditor`/email (`[Physiotherapy]` subject)/`/note` save; CDMSS off. physiotherapist clinician_type + picker already shipped in S4. **All 5 note types now live.** 👤 **Pilot physiotherapist account UNPROVISIONED — L3 name still TBD**; admin can create it anytime via the Physiotherapist selector. **Pending V device test** (once a physio account exists). |
| V2.S6 — read-from-new migration | ✅ SHIPPED | tag `v2-s6-read-migration-shipped` @ `3c44338`, build green, **verified incl. live doctor login**. **S6a** (`0a42131`): migration `0012` Phase-C backfill (refresh all clinician from doctor, clinician_type preserved) + auth path switched doctor→clinician — PIN login (`api/auth/pin`), lockout (`lib/lockout.ts` 4× UPDATE), doctor page `findDoctor`. **S6b** (`3c44338`): display/email/admin reads → clinician (send/resend sender name, /process diarize join, encounter admin joins, doctor-detail, dashboard counts, admin doctors GET) + admin **Doctors→Clinicians** rename. Verified: reset-PIN→sync→clinician→login `{ok:true}`, wrong-PIN lockout on clinician, admin list reads clinician (Afshan=dietitian preserved), encounter detail name join, dashboard 200. **Writes still target doctor + sync** (FK + dropped in S8). **Deferred to S8 sweep:** lib/clinician.ts (sync source), admin create INSERT + edit/reset-pin/rotate-url UPDATEs, voice identify/enroll + email-url + mutation-return SELECTs. Note: dr-vinay PIN reset to 6110 during verification. |
| V2.S7 — admin polish + recipient routing | ✅ SHIPPED (filters/chips) | tag `v2-s7-admin-polish-shipped` @ `3c24ba0`, build green. Admin Encounters list: `note_type` in query + server-side `?note_type=` filter (verified) + filter dropdown + inline NoteTypePill chip. Admin Clinicians list: clinician_type filter dropdown. Doctor Library: note_type chip per encounter. **Per-note-type recipient AUTO-defaults (OR/anesthesia/pathology, dietetic/physio routing) DEFERRED per O3** — v2.0 recipient model is per-clinician+global+ad-hoc with no patient-identity/role tagging; auto-routing is v2.3+. note_type now visible in send context. |
| V2.S8 — cleanup + capstone (drop `doctor` table) | ✅ COMPLETE | tag `v2-s8-complete` @ `a022258`. **S8a** (`4d56c6e`): final read sweep. **S8b** (V took Neon snapshot + go-ahead): all writes switched doctor→clinician (create/[id] PATCH/reset-pin/rotate-url/bootstrap; sync calls removed), migration `0014_repoint_fks` (DO-block repoint encounter/voice_print/recipient_per_doctor/pin_attempt → clinician), migration `0015_drop_doctor` (**DROP TABLE doctor**). Verified live post-drop: doctor login 200, a brand-new clinician with NO doctor row logs in, admin create/edit/list/dashboard/encounter-detail all 200. **`clinician` is now the sole identity table. 🎉 V2.0 COMPLETE.** (Minor cleanup left: schema.ts still declares a stale `doctor` pgTable — cosmetic, raw SQL everywhere; a soft-deleted test clinician `doc_cqcwzd7q` exists.) |
| Post-v2.0 KB ingestion | ⬜ | v2.2 nutrition KB, v2.x rehab + surgical KBs |

---

## 4. v2.1 diarization sprints (PRD locked, NOT started)

PRD: `ETA-V2-PRD.md` §20.10. Lands after v2.0 pilot stability (~4-6 wks post-v2.0).

| Item | Status |
|---|---|
| V2.SD.0 → V2.SD.7 (schema, enrollment wizard, live pipeline, submit pipeline, OSD, Speakers UI, threshold revalidation, capstone) | ⬜ |
| Pilot physiotherapist name (shared dependency with v2.0 S5) | ⬜ 👤 |

---

## 5. v1 backlog (deferred during v1 scoping)

| Item | Status | Notes |
|---|---|---|
| LIVE TAIL via SSE on trace dashboard | ⬜ | Currently 10s polling |
| Abbreviation expansion fallback | ⬜ | llama3.1:8b sometimes expands BD→twice-daily |
| `/transcribe/cleanup` + `/whisper-chunk` trace writers | ⬜ | Need sampling first (per-utterance volume) |
| `/admin` obscure-path middleware | ⛔ | Locked WON'T-DO (internal demo) |
| Inbound Resend reply handling | ⛔ | Locked deferred indefinitely |
| Force-logout admin endpoint | ⬜ | Minor |

---

## 6. Pending V manual actions (housekeeping)

| Item | Status | Notes |
|---|---|---|
| Smoke-test B6/B7/B10/B11 with a real >3-min recording | ⬜ 👤 | Confirms Whisper delta-upload + empty-note guards hold |
| Click "View in app" on an old email (B9) | ⬜ 👤 | Confirms bare-route redirect on pre-fix emails |
| Rotate/revoke `eta-cors-admin` R2 token | ⬜ 👤 | TTL expired ~3 June 2026; idle, harmless |
| Update `APP_URL` env → evenscribe.app | ⬜ 👤 | Cleanup; `canonicalAppUrl()` covers it |
| Update R2 CORS to drop legacy domain (if retiring `eta.llmvinayminihome.uk`) | ⬜ 👤 | Cleanup |
| ~~Add `SARVAM_API_KEY` to Vercel env~~ | ✅ | Done 29 May (Production+Preview, Sensitive) + redeployed. |
| ~~B12: Sarvam rejected `audio/webm;codecs=opus`~~ | ✅ FIXED | sha `76cff00` — `lib/sarvam.ts` strips MIME params (Sarvam only accepts bare `audio/webm`/`audio/mp4`). Was the cause of V's failed Kannada test. Verified live. See `ETA-BUG-LOG.md` §B12. |
| ~~Submit-time Sarvam pass on full R2 file (Batch)~~ | ✅ SHIPPED (v2) | Done — `/process` runs Sarvam STT-Translate Batch on the whole R2 audio (full-context English + medical prompt) → replaces transcript_raw before note-gen. Accuracy fix + safety net. Verified on real audio (4.6s). |
| ~~Single code-mixed live box (no dual-box jumping)~~ | ✅ SHIPPED (v2) | Rolling switched to Sarvam `codemix`; one continuously-scrolling box; Deepgram/Whisper hidden (still log to transcription_run). |
| ~~B13: iOS Safari recording froze after Pause→Resume~~ | ✅ FIXED | sha `507ec3b` — iOS `MediaRecorder.resume()` doesn't restart timeslice emission. Fix: pure soft-pause (no native pause/resume; recorder runs continuously, paused chunks dropped). `ETA-BUG-LOG.md` §B13. |
| **v2 — Device re-test (incl. pause/resume)** | ⬜ 👤 | Re-record a Kannada/Hindi consult: (1) single flowing box, no jumping; (2) translation noticeably better; (3) Pause → switch audio → Resume works (no freeze) and the paused part is excluded. Tags `mt-multilingual-v2-shipped` @ `69bbc07`, B13 @ `507ec3b`. |
| (backlog) True word-by-word live | ⬜ | Current live box is ~10s near-live blocks. Smooth streaming needs the Cloudflare Worker WS relay (deferred). |
| (backlog) Medical-term hinting beyond the prompt | ⬜ | Sarvam batch already gets `SARVAM_MEDICAL_PROMPT`. If accuracy still short, explore Sarvam pronunciation/glossary or an LLM post-edit. Measure first. |

---

## 7. Known caveats carried forward (not bugs, but watch)

- Whisper rolling on long recordings: delta-upload redesign (B7) holds; B6 longer-of-two pick is the backstop.
- CDMSS is soft-fail tiered — never blocks an encounter.
- `canonicalAppUrl()` masks the stale `APP_URL` env — don't remove before the env is fixed.
- Diarize pinned stack is load-bearing — never `pip install -U` that venv.

---

**No critical open bugs** as of 31 May 2026 (evening), `d815c20`, all 6 services green. v1 + multilingual + diarization + v2.0 (5 note types) + voiceprint retention A/B + the STT Engine Lab (L0–L7) are all shipped and live. The biggest active threads are now all **pending-V validation, not build work**: (1) STT Lab — label gold + set per-engine cost to light up accuracy + cost scoring; (2) voiceprint passive-capture real-encounter smoke test; (3) provision the pilot physiotherapist; (4) device-test all 5 note types. Candidate next builds: a local AI4Bharat IndicConformer STT engine (1 adapter + 1 row), diarization v2.1 live-pill/relabel tail, post-v2.0 KB ingestion. **Authoritative handoff: `ETA-CARRYOVER-PROMPT-31-MAY-2026.md`.**
