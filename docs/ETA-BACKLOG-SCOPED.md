# ETA — Backlog (scoped, ordered by difficulty)

**Created 2 Jun 2026.** Source = B19 audit register (`ETA-BUG-LOG.md`). Excludes the 3 deferred **security P0s** (seed-team auth, admin RBAC gates, finalize-upload key-binding) per V, and 2 already-shipped items (RecoveryModal resubmit `cbe9bfe`; the `getUserMedia` 16 kHz→`{ideal}` constraint `9d4e86d`). Order is easiest/lowest-risk → hardest/highest-risk. Each ships with the standard loop (build green → deploy → verify live sha + `npm run smoke` → tag) and now also the vitest + Playwright e2e safety nets for recording-path changes.

Legend — **Effort:** XS/S/M/L · **Risk:** low / med / high (blast radius on the clinical path).

## Tier 1 — trivial, low-risk (non-clinical; data/STT-lab/config)
1. **Crypto-strong PINs** — `app/api/admin/doctors/route.ts:105`, `reset-pin/route.ts:34`. Replace `Math.floor(Math.random()*10000)` with `crypto.randomInt(0,10000)`. Effort XS · Risk low · Verify: unit test + build.
2. **Lazy `DATABASE_URL`** — `lib/db-neon-http.ts:5`. Stop the eager `DATABASE_URL!` at import (match `lib/db.ts`'s lazy accessor). Effort XS · Risk low · Verify: build + smoke.
3. **`voice_sample` unique index** — migration `0024` adding a unique `(clinician_id, source_encounter_id)` (partial, where source='passive') to harden passive-capture dedup. Effort S · Risk low (additive) · Verify: run-migrations + dedup still works.
4. **STT scoring no longer hides failures** — `lib/stt/scoring.ts:192` + scoreScribe/scoreEncounter. Don't stamp `scored_at` when the judge is unavailable (so it retries, not silently "scored" with null); surface an `extract_status` so a failed term-extraction is visible, not a silent 0. Effort S · Risk low (STT-lab only) · Verify: unit/logic + STT-lab page.

## Tier 2 — small, contained (server or test; minimal clinical-path risk) — ✅ SHIPPED 2 Jun 2026
Commits: server `53039a8` (#5,#10) · client `78a5eb6` (#6,#7,#9) · test `d60222a` (#8). All built green, HEAD `d60222a` live on evenscribe.app, smoke 9/9.

5. ✅ **Sarvam batch timeouts** — `lib/sarvam.ts`. Wrapped all 7 batch-job fetches in a `tfetch()` helper combining the caller's `opts.signal` with a per-request deadline (control 20s / transfers 60s / status-poll 15s; no reliance on `AbortSignal.any`/`.timeout`). A hung Azure transfer can no longer stall `/process` to maxDuration; a single slow status poll retries within `maxWaitMs` instead of aborting the job. `53039a8`.
6. ✅ **Deepgram header chunk preserved** — `lib/use-deepgram-live.ts`. Pre-open queue cap now evicts the oldest *non-header* chunk (`splice(1,1)`) instead of `shift()`, keeping the WebM/opus container header so Deepgram can decode the drained clusters after a slow WS open. `78a5eb6`.
7. ✅ **Preflight `AbortSignal.timeout` guard** — `PreflightCheck.tsx`. Added `timeoutSignal()` (feature-detect `AbortSignal.timeout`, fall back to `AbortController`+`setTimeout`) so Safari < 16 / iOS 15 no longer throws and mislabels a healthy backend; fixed the stale "chunks always land in IndexedDB" docstring. `78a5eb6`.
8. ✅ **mic-denied e2e** — `tests/e2e/recording.spec.ts`. New spec overrides `getUserMedia` to reject with `NotAllowedError`; asserts the `permission_denied` UI (status line + guidance + Reload) renders and nothing uploads. Closes the TODO. **Test-only; pending first CI run (nightly E2E 02:00 UTC or manual dispatch).** `d60222a`.
9. ✅ **RecoveryModal hides already-submitted** — `lib/chunk-store.ts` + both submit paths. Added `markEncounterSubmitted()` writing a best-effort sentinel row *before* purge; `listEncounterSummaries()` filters marked encounters so a failed post-submit purge can't resurface an uploaded consult as "unfinished" (dup-upload risk). Marker shares `encounter_id` so `purgeEncounter()` removes it too. `78a5eb6`.
10. ✅ **Whisper rolling-buffer race** — `whisper-chunk/route.ts`. A transient R2 read of the ephemeral live-transcript buffer no longer hard-fails the append mid-consult; treat the window as a fresh buffer and continue (saved/uploaded audio is independent — nothing the note needs is lost). `53039a8`.

## Tier 3 — medium (logic / concurrency; needs care) — ✅ SHIPPED 2 Jun 2026
Commits: STT-lab `f9ecb18` (#11,#12,#13 + migration 0025) · email `b9fde47` (#14,#15) · reaper `595d16c` (#16). All built green, HEAD `595d16c` live, smoke 9/9, migration 0025 applied (errored=null). Verified live: a drain ran clean through the new atomic-claim SQL; the reaper found + reaped a real 7-day-wedged `processing` encounter (`enc_xku9h7u4k8` → failed), re-check stuck_count=0.

11. ✅ **scribePending infinite-loop fix** — `scoring.ts`. `scoreScribe` now stamps a done-marker (`scored_at` + `scribe_skip` reason) on ALL of an encounter's scribe runs on the two non-scoreable early returns (empty reference note; no candidate rows) via new `markScribeDone()`. Junk/empty-note encounters are no longer re-selected by `scribePending` forever. `f9ecb18`.
12. ✅ **STT daily-budget enforces** — `fanout.ts`. New `estimateCostUsd()` fills `cost_usd` at ASR + scribe insert: adapter cost if reported, else `cost_per_min_usd × duration`, else a conservative default for an unpriced PAID engine (free = 0). `todaySpendUsd → allowPaid` now actually accrues and the `$5/day` cap can pause paid engines (was a no-op while `cost_usd` was null). `f9ecb18`.
13. ✅ **`drainFanout` atomic claim** — `fanout.ts` + migration 0025. Claims in ONE `UPDATE … WHERE encounter_id IN (SELECT … FOR UPDATE SKIP LOCKED) RETURNING` (proven on Neon HTTP) and reclaims ONLY stale `running` (`started_at` > 5 min / null) instead of blanket-resetting every running job. Two concurrent drains can't grab the same encounter → no more double-processing for `dedupRuns` to mop up. Migration 0025 adds `stt_fanout_job.started_at`. `f9ecb18`.
14. ✅ **Duplicate emails on /send retry** — `send/route.ts`. Before emailing a recipient, skip if that exact (encounter, recipient) was sent in the last 90s (status queued/sent/delivered/opened), returned as `deduped:true`. Kills double-click/retry dupes; a deliberate resend past the window — or the separate admin `resend` route (left untouched) — is never blocked. `b9fde47`.
15. ✅ **Resend webhook hardening** — `webhooks/resend/route.ts`. (a) Rejects events whose `svix-timestamp` is >5 min off (replay guard). (b) Status transitions are now guarded: a terminal NEGATIVE status (bounced/complained/failed) is sticky — a late/out-of-order delivered/opened can't overwrite it (open timestamp still recorded via COALESCE); stamps `updated_at`. `b9fde47`.
16. ✅ **Stuck-`processing` reaper** — new `POST /api/admin/reap-stuck` (Bearer MIGRATION_SECRET or admin cookie; `{dryRun,minutes}`). Sweeps encounters wedged in `processing` beyond `minutes` (default 30) → `draft_partial` if a note exists, else `failed`; writes a `system` audit_log row. `595d16c`. **Now scheduled hourly via Vercel Cron** (`vercel.json` `0 * * * *` → GET handler authed by Vercel's un-spoofable `x-vercel-cron` header / optional `CRON_SECRET`) — `4390fdd`. Verified live: reaped a real 7-day-wedged encounter; unauthenticated GET returns 401.

## Tier 4 — higher-risk (clinical recording path) — ✅ SHIPPED 2 Jun 2026 BEHIND FLAGS (default OFF)
Commit `9dab463` (build green, HEAD live, smoke 9/9). ALL three behind `NEXT_PUBLIC_*` flags in `lib/live-flags.ts`, each defaulting OFF so prod behaviour is byte-identical until set in Vercel + device-tested. **Flags + per-item device-test checklist = `ETA-TIER4-FLAGS-DEVICE-TEST.md`.** Verify-on-device still PENDING-V (sandbox/CI can't drive iOS WebKit or real mic).

17. ✅ **Trim unbounded in-memory chunk buffers** — `NEXT_PUBLIC_ETA_TRIM_LIVE_BUFFERS`. whisper-rolling + sarvam-rolling drop consumed chunks off the front via a `baseRef` absolute-index offset after the watermark advances (header preserved in R2 / `headerRef`); speaker-identify caps its tail buffer (`SPK_MAX_CHUNKS=120`). Canonical upload copies (IDB + `chunksMemRef`) untouched. Flag OFF ⇒ `baseRef=0`, no splice, identical. `9dab463`.
18. ✅ **Deepgram live reconnect** — `NEXT_PUBLIC_ETA_DEEPGRAM_RECONNECT`. `use-deepgram-live` reconnect effect refactored to `connect()`/`scheduleReconnect()` (hoisted fns): exp backoff 1/2/4/8s, max 5, token re-mint on abnormal close, backoff reset on open. `scheduleReconnect()` no-ops when flag off ⇒ close/error handling identical. `9dab463`.
19. ✅ **Desktop-Safari streaming starvation** — `NEXT_PUBLIC_ETA_SAFARI_STREAMING_GUARD`. `RecordingScreen` extends the iOS worklet-skip to desktop Safari via new pure+tested `detectDesktopSafari()`/`isDesktopSafariUserAgent()` in `platform.ts`. Flag OFF ⇒ `STREAMING` computed exactly as before. `9dab463`.

## Tier 5 — spread-out review — ✅ SHIPPED 2 Jun 2026
Commit `1857130` (build green, HEAD live, smoke 9/9, scanner clean).

20. ✅ **Swallowed-error triage + CI gate** — all 28 empty `catch {}`/`.catch(() => {})` handlers flagged by `npm run check:silent` were, on inspection, deliberate best-effort swallows (audit/status side-writes, NDJSON tail-parse skips, tracing, AudioContext close, optional-body parse — incl. the `process/route.ts:90` `force`-flag parse, which is already guarded by `req.json().catch(()=>({}))`). Each now carries an honest `/* intentional: <reason> */` comment (comment-only; zero runtime change), so the scanner no longer flags it. With the baseline at **0**, `check:silent` is promoted to a **hard CI gate** in `ci.yml` (after the vitest step) — any NEW empty handler in lib/components/app fails CI until handled or annotated. `1857130`.

---

## ✅ BACKLOG COMPLETE (2 Jun 2026) — all 20 non-security items shipped
Tier 1 `0f40743` · Tier 2 `53039a8`/`78a5eb6`/`d60222a` · Tier 3 `f9ecb18`/`b9fde47`/`595d16c` (+ reaper cron `4390fdd`) · Tier 4 (flag-gated) `9dab463` · Tier 5 `1857130`. HEAD `1857130` live on evenscribe.app, every commit built green, smoke 9/9 throughout, migrations 0024+0025 applied. **Remaining open work (not in this backlog):** the 3 security P0s + admin-login lockout (PARKED by V); device-test + activate the Tier-4 flags (see `ETA-TIER4-FLAGS-DEVICE-TEST.md`); first CI run of the mic-denied e2e spec (#8) — nightly 02:00 UTC or manual dispatch.

## Parked (security — deferred by V, tracked in B19)
- seed-team unauth super-admin creation; missing admin RBAC role gates; finalize-upload key-binding. **Plus** admin-login has no lockout/rate-limit (`admin/login/route.ts`) — security-adjacent, parked here too unless reprioritized.
