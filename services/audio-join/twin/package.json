{
  "name": "eta-audio-join-twin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "JOIN-SHARD twin front: the Worker's job (R2 over the S3 API) in front of the unchanged join container. Deployed by hand on a box; NOT the app's package.json and NOT the Worker's.",
  "scripts": { "start": "node server.mjs" },
  "dependencies": { "@aws-sdk/client-s3": "^3.700.0" }
}
