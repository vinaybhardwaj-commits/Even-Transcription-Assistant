# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (DO NOT COMMIT)
.env*.local
.env.development.local
.env.test.local
.env.production.local
secrets.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
tests/e2e/.auth/

# Room Bench PRD/mockup — local-only docs (public repo)
docs/room-bench/
docs/brain/


# Warehouse fixtures (fuse slice 3) — a REAL extract holds individual_uid and is never
# committed. The one synthetic fixture beside scripts/load-warehouse-fixture.ts is the
# exception: fake uids, and it is what tests/unit/warehouse-join.test.ts runs against.
fixtures/warehouse/*.json
!fixtures/warehouse/synthetic-19-aug.json
.env*

# Vendored service dependencies — services/*/node_modules is install output, never source.
# (One of these is a 134 MB workerd binary that GitHub refuses outright.)
services/*/node_modules/

# --- Tunnel-facing handoff docs: NEVER commit ---
# This repo is PUBLIC and the home inference endpoints have no auth.
# These live in docs/handoff/ on disk (the bus) but must not enter git.
# Enforced by .githooks/pre-commit. Ruling: V, 12 Sep 2026.
docs/handoff/ETA-LLM-TUNNEL-API-MANUAL-*.md
docs/handoff/ETA-AUDIT-B-AUDIO-STT-TUNNEL-*.md
docs/handoff/ETA-RESEARCH-ROUTER-RELAY-EMOTION-*.md
docs/handoff/ETA-TIER2-SPEC-v1.0-MCP-REDESIGN-*.md
docs/handoff/ETA-TIER2-SPEC-SLICE-C-*.md
