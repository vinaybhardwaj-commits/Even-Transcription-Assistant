{
  "name": "even-scribe-brain",
  "version": "0.1.0",
  "private": true,
  "description": "Even Scribe Ambient Encounter Brain — Cloud Run service (skeleton, PRD build-order step 1). Not part of the Next.js app.",
  "type": "module",
  "main": "dist/server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "pg": "8.13.1"
  },
  "devDependencies": {
    "@types/node": "22.10.7",
    "@types/pg": "8.11.10",
    "typescript": "5.7.3"
  }
}
