# Room Recorder working scope

Read `../../docs/handoff/ETA-ROOM-RECORDER-BUILD-B-COMPLETION-27-AUG-2026.md` before changing this
app. It governs current execution when older plans conflict.

- Keep the accepted unsigned browser-replacement path unchanged by default.
- Execute Build B steps 4 through 12 in the ratified order without changing the server contract.
- Preserve the completed unsigned archive and local-derivation checkpoints as development evidence.
- Use only synthetic or explicitly non-clinical audio in the unsigned development build.
- Never represent the published fixed test key as production confidentiality.
- Session-global samples cross midnight; each lane receives a fresh root and stream UUID per IST day.
- Capture belongs in the resident app; FFmpeg is the only production helper.
- Observe every security, physical-coordination and stop rule in the governing handoff.
